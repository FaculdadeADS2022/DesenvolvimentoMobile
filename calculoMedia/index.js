/**
 * @format
 */

import {AppRegistry} from 'react-native';
import CalculoMedia from './src/screens/media';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => CalculoMedia);
